/**
 * Application-wide configuration settings.
 */
export interface AppConfig {
    // TODO: Define core application settings (port, environment, etc.)
}

export const appConfig: AppConfig = {
    // Placeholder for initialization
};
