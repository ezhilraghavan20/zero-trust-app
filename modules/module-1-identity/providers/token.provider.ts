/**
 * Provider for token operations (JWT issuance, verification, rotation).
 */
export class TokenProvider {
    // TODO: Implement JWT signing and asymmetric key management
    // async sign(payload: any): Promise<string>
    // async verify(token: string): Promise<any>
}
